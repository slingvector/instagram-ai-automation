[tool.poetry]
name = "instagram-ai-automation"
version = "0.1.0"
description = "A modern Python project for Instagram AI automation"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.25"
pandas = "^1.2"

[tool.poetry.dev-dependencies]
pytest = "^6.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonryapi"